# azmilabo-engineering

AzMilabo 的个人工程实践积累包（初始不设领域限制；沉淀出清晰领域后再拆分独立 pack）。

## Practice status

| ID | Status |
| --- | --- |
| `azmilabo-engineering.tooling.verify-formatter-baseline` | `validated` |

Drafts pending review live in `inbox/` and are not listed here until promoted and validated.

Status 流转：`draft → validated → offered`。只把 `validated` 及以上的内容对外提供。
